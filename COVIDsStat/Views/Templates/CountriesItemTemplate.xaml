<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             x:Class="COVIDsStat.Views.Templates.CountriesItemTemplate">
    <ContentView.Content>
        <Grid Padding="10,8" ColumnSpacing="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ffimageloadingsvg:SvgCachedImage Aspect="AspectFill" Grid.RowSpan="2"
                                              WidthRequest="100" HeightRequest="60" Source="{Binding Flag}"/>
		                
            <Label Grid.Column="1" VerticalOptions="CenterAndExpand"
                    Text="{Binding StrCountry}" FontAttributes="Bold"
                    TextColor="#363636" FontSize="Large"/>

            <Label Grid.Row="1" Grid.Column="1"
                    VerticalOptions="StartAndExpand" TextColor="Gray"
                    Text="{Binding TotalCases,StringFormat='Total cases: {0:N}'}" FontSize="Medium"/>
        </Grid>
    </ContentView.Content>
</ContentView>
